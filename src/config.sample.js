/*
    Change these values to whatever works
    on your environment and then build.
*/
export const apiBaseUrl = "http://localhost:8080/project/api/";
export const googleApiKey = "use-your-own";

//Set this to `true` to test without the server running!
export const useMockApi = false;